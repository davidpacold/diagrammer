# CloudFlare Architecture Diagram Tool

This project is an interactive system architecture diagram tool built with React and hosted on Cloudflare Pages.

## Tech Stack
- **Frontend**: React + Vite
- **Diagram Library**: React Flow
- **Styling**: Tailwind CSS
- **Hosting**: Cloudflare Pages

## Project Structure
- `/src` - React application source code
- `/src/components` - React components (DiagramCanvas, ToggleSidebar, etc.)
- `/src/data` - Component definitions and initial diagram data
- `/public` - Static assets

## Key Features
- Toggle visibility of architecture components
- Drag and drop diagram elements
- Hover tooltips with component details
- Session-based state (no persistence)

## Development
```bash
npm install
npm run dev
```

## Deployment
```bash
npm run build
npx wrangler pages deploy dist
```

## 📚 Maintenance Documentation for Claude Code

**For modifying diagram components (adding/removing/editing services):**

1. **Quick Reference**: See `.claude/QUICK_EDIT.md` - Fast templates and commands
2. **Full Guide**: See `.claude/MAINTENANCE.md` - Comprehensive instructions
3. **Examples**: See `.claude/EXAMPLES.md` - 10+ complete walkthroughs

**TL;DR**: Edit `src/data/components.js` only. That file contains all component definitions and connections.

**Common Tasks**:
- Add component → Copy template from QUICK_EDIT.md
- Remove component → Delete component + all its connections
- Change position → Update `position: { x, y }`
- Change zone → Update `zone: 'public'` or `'private'`

Always test with `npm run dev` before committing!
